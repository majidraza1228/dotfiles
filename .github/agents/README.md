# Agent Standards

## Why helpful

Custom agents give Copilot role-based behavior (for example: reviewer, security auditor, architect).

## How to use

1. Create one role per agent.
2. Define responsibilities, boundaries, and output format.
3. Invoke agents from chat using `@agent` or agent picker, or create with `/create agent`.

One role per agent, checklist-driven outputs, clear scope boundaries.
