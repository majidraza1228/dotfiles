# Skills Standards

## Why helpful

Skills package specialized capabilities (instructions + resources) that Copilot can use when relevant.

## How to use

1. Define one capability per skill.
2. Specify triggers, required inputs, and expected outputs.
3. Create from chat with `/create skill` and scope to user/workspace.

Each skill should have explicit trigger conditions, inputs, and outputs.
