# MCP Server Standards

## Why helpful

MCP servers connect Copilot to external tools (GitHub, tickets, data systems) so it can execute workflows instead of only suggesting code.

## How to use

1. Keep one server config per file.
2. Use least privilege scopes.
3. Use environment variables for credentials.

Use least privilege and environment variables for credentials.
