# Codex MCP Servers

Use this folder for repository-approved MCP snippets and setup notes.

## Standards

- Keep active MCP configuration in `.codex/config.toml`.
- Store example snippets here so teams can review them before enabling.
- Use environment variables for tokens and secrets.
- Document the minimum required scopes beside each server snippet.

## Included example

- `context7.example.toml` — developer documentation MCP server
