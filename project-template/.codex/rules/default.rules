prefix_rule(
    pattern = ["git", "push", "--force"],
    decision = "forbidden",
    justification = "Do not rewrite shared history from this repository."
)

prefix_rule(
    pattern = ["npm", "test"],
    decision = "allow",
    justification = "Standard repository test command."
)
