# Codex Rules

Put reusable Codex approval and safety rules here.

## Standards

- Use `prefix_rule(...)` entries for durable command policies.
- Prefer narrow command prefixes over broad wildcards.
- Add `justification` text for rules that block or prompt.
- Keep user- and machine-specific approvals out of shared project files when possible.
